# 検証結果と残作業

確認日: 2026-09-24。作品版1.20.0。今回の起点はmaster fdeb6bb（PR #43反映済み）です。

## 今回のセル種整備

24種類のプリセット、日本語名と用途、表面模様、毒沼の進入イベントと処理を正本へ追加しました。既存の361セルを対応する仕掛けの種類へ分類し、全33マップの初期通行値と配置物を維持しています。氷床の滑走、崩壊床と保存、安全地帯、固定浅水・深水、腐食床の装備への影響を実装しました。

`npm run check` は全684試験成功、失敗0・skip 0です。`npm run build:scenarios`、`npm run check:docs`（52現行文書・2506ローカルリンク・13保存原文）、`git diff --check` も成功しました。新規10試験で、原稿と配布の一致、毒の進入と保存再開、滑走の歩数とイベント停止、崩壊の成功・失敗・保存・不正保存、安全地帯と強制イベント、水面と通行、腐食と洗浄、関連仕掛けの不足検出、表面描画と数値検証、戦闘・転送による滑走中断を確認しました。既存の地形パッチ検証は、追加した表面模様を含めて項目単位の合成を確認するよう更新しました。旧シナリオの構造比較は新規 `cell.poison_step` だけを明示して分離し、その命令全文を検査します。

実Chromiumの `tools/browser-cell-smoke.mjs` で、24候補・用途説明、塗布と模様、毒沼のJSON出力・処理編集、仕掛け未設定の塗布拒否、1440・900・390・320px幅、ゲーム本体のCanvas描画を確認しました。統合編集の既存 `tools/browser-map-smoke.mjs` も成功しています。全33マップのtiles・objectsが起点masterと一致することを別途比較しました。公開Pagesへの反映はマージ・配信後です。

以下は以前の作業記録です。

## 今回の統合マップ編集

迷宮 → マップから地形・入口・イベント・仕掛け・接続を編集する画面を追加しました。各操作の編集JSON、共有セル種の影響範囲、複数原稿の下書きと一括出力を表示します。接続先の新規マップをその場で作り、A/Bを配置図で指定できます。[仕様](CONFIG_EDITORS.md)、[正本](CONFIG_EDITOR_SOURCES.md)、[公開URL](CONFIG_EDITOR_URLS.md)。

`npm run check` は全674試験成功、失敗0・skip 0です。`npm run check:docs`（現行52文書・2493ローカルリンク・13保存原文と集計）、`git diff --check` も成功しました。新規6試験で、壁灯の正本選択とマップ保持、セル種の影響範囲、新規マップ3原稿と接続のUndo、不正な接続と未反映時の出力停止、地形変更で壊れた接続・未接続マップの検出、入口と接続の選択を確認しています。接続検証はゲームと共通の純粋関数へ移しました。ゲーム原稿225件とdataの変更はありません。

実Chromiumの `tools/browser-map-smoke.mjs` で、/adv/配下での統合画面起動、地点上書きと共有セル種、壁灯の名前・面移動、新規接続先とA/B配置、4原稿の全文出力とコピー、Undo、1440・900・390・320px幅を確認しました。既存の `tools/browser-config-smoke.mjs` も回帰確認に成功しました。公開Pagesへの反映はPRのマージと配信後です。

以下は以前の作業記録です。

## 今回の分野別原稿編集

225個の編集HTMLを新画面へ切り替えました。対象ごとの設定、名前付き参照、配置図とセル・エッジ操作、条件と処理ブロック、比較一覧、素材・効果・音の確認、複数原稿のUndo/Redo・差分確認・全文コピーを実装しています。編集対象と出力先を画面と[正本の一覧](CONFIG_EDITOR_SOURCES.md)に明記しました。

`npm run check` は全668試験成功、失敗0・skip 0。新規10試験で225原稿の参照、複数ファイル操作の原子性、未知項目の保持、セル上書きとエッジ条件、イベントと処理の同時作成、新規マップ・セル・所属一覧の一括作成、未読ファイルからの参照、技能消費と出力、検証中の不正入力、入れ子処理と消費追加、全分野の画面生成を確認しました。全225ページの起動後に原稿が変更されないことも確認しています。

ローカルの実Chromium 131でtools/browser-config-smoke.mjsを実行しました。Pagesと同じ/adv/配下で検索、名前付き編集、壁灯の面移動、セルの塗り替え、技能のMP変更、3原稿の出力、ネイティブのコピー、Undoと出力無効化、エラー表示、1440・900・390・320px幅が成功しています。効果の実アニメーション、音符の簡易合成、32候補からの壁・装置素材選択と出力も確認しました。日本語フォントを使用した画面も確認しました。公開Pagesへの配信確認、すべての効果・音の聴感確認、全シナリオの編集後プレイは今回の確認に含めません。

元原稿にある探索演出4参照は注意として保持し、新規の参照切れは出力を止めます。ゲーム原稿225件とdataは変更せず、照度の純粋関数だけを別モジュールへ分離して共用しています。作品版・セーブ形式・通常点灯と遭遇・消費時の明示選択は維持します。

`npm run build:scenarios`、`npm run check:config`、`npm run check:docs`（現行52文書・2484ローカルリンク・13保存原文と集計）、`git diff --check` も成功しました。全体再生成後も原稿225件と配布dataは起点とバイト単位で一致します。

以下は以前の作業記録です。

## 今回のたいまつと遭遇

通常点灯をくらがり除けに統一し、ordinary／wardの通常遭遇倍率を1にしました。消灯中の無条件襲撃を削除し、既存の7成功歩ごとの遭遇判定へ接続しました。基本確率22％と敵種の重み付き候補を分離し、現状の候補はくらがり100％です。深火の遭遇抑制、壁灯の防護、燃料切れ後の状態での判定、q001の独立した消灯イベント戦闘を維持しています。[仕様](KAGARIBI_DUNGEON.md)。

全658テスト成功。新規7件で通常点灯時の通常魔物、消灯時の確率境界、抽選のタイミング、燃料切れと壁灯、深火と呼び寄せの重複、追加敵種の重みと撃退・保存、不正定義を確認しました。q001の既存2結末試験では、乱数が外れる値でも消灯イベント戦闘が始まり、物語の継続情報を持つことを確認しています。汎用の条件付きイベント試験は専用の試験定義へ変更し、旧3D原稿は編集せず試験読込アダプターで旧threat形式を変換しました。

データ検査（200クエスト・33マップ・2319スクリプト）、静的検査、設定編集検査（225原稿）、文書・リンク・生成スナップショット検査が成功しました。今回の変更はCoreとデータの動作試験で確認し、実ブラウザ・公開版の通し操作は未実施です。旧内容版のセーブは移行せず、新規開始になります。

以下は以前の作業記録です。

## 今回の戦闘UI整理

戦闘の左下に行動者と階層式コマンド、右にログを配置しました。最上位は攻撃・防御・スキル・アイテム・逃走、スキル／アイテムは個別候補を開き、単体対象だけ追加で選びます。未確定の選択・取消では資源や手番を消費せず、一段戻って元の項目へフォーカスします。英語のラウンド表示、常設キー説明、使用不能理由の全件列挙、探索装置summaryをコマンド欄から除きました。

下部高さは通常28%・最低200px、低い横画面48%を維持し、左右枠を同じ高さに揃えています。候補一覧は見出しを残して内部スクロールします。戦闘中会話も左操作・右本文、従来ビューも同じ階層と左右配置です。Core、Application、原稿・配布データ、作品版と保存形式は変更していません。

`npm run check` は全653テスト成功、失敗0・skip 0。両ビューでカテゴリから候補・対象へ進み、一段ずつ無消費で戻れること、元のフォーカス、技能の確定一回、自己対象の防御、次手番の初期選択を追加検証しました。既存のキー変更試験は、削除した常設説明の存在確認を次手番のフォーカス確認へ変更し、割り当て変更後の確定・取消を維持しています。200クエスト・33マップ・2319スクリプト・素材参照、225設定原稿と編集HTML、静的検査も成功しました。

`tools/browser-battle-smoke.mjs` をローカルの実Chromium 133で実行し、1280×856・900×700・390×844・320×568・844×390を確認しました。実GameEngineによるスキル／アイテムの階層選択、対象取消の無消費、回復薬の一回消費と回復、次手番へのフォーカスを検証しています。候補数の負荷確認は投影モデルだけに24件の仮スキルを追加し、左右の座標、同じ固定高さ、上下キーによる末尾への到達、内部スクロール、横にはみ出さないことを実画面で計測しました。本編で24技能を習得・使用した試験ではありません。戦闘中会話と従来表示も確認し、ブラウザ例外・HTTPエラーは0件です。

既存の `tools/browser-scene-smoke.mjs` も同じChromiumで成功しました。通常会話の固定高さ・本文24pxの実寸ページ送り・18選択肢・人物配置・従来カード・探索記号を確認しています。スクリーンショットはローカルの描画を確認するために使用しました。公開版の通し操作・全戦闘・音声は今回の確認範囲に含めません。仕様は[背景内ビュー](ui/IN_SCENE_VIEW.md)、[共通キー操作](ui/KEYBOARD_CONTROLS.md)。

`npm run check:docs` は現行50文書・2467ローカルリンク・13保存原文とデータ集計の一致を確認しました。`git diff --check` も成功しています。

## PR #39の設定JSON編集

225原稿JSON（クエスト配置200、迷宮13、直下12）をconfigへ移動し、一対一のHTML、検索一覧、詳細フォーム、全体／部分JSON編集、追加・削除・配列移動、ファイル読込、Undo、検証後の全JSONコピーを追加しました。生成器と現行文書の参照も移しました。移動前後の原稿225件はバイト単位で同一です。通しのbuild:scenarios後もdataとsrcは基準masterと差分がなく、内容版1.18.0と保存形式を維持しています。

`npm run check` は全649テスト成功、失敗0・skip 0。新規16件で、未知の深い項目・非表示ページ・キーと配列順の保持、構文／重複キー／予約名／数値精度／容量上限、不正な操作の原子性、DSL・条件・数値範囲・ファイル内参照、未知のSchema規則と解析上限の拒否、フォーム編集、直接JSONの反映、Undo、再検証と古い出力の無効化、クリップボード失敗時の全文選択、非同期読込中の編集保持、読込失敗からの回復を確認しました。

`check:config` は全225原稿を検証し、225対応HTMLと一覧の生成一致、JSON出力の安定を確認します。通常checkとCIへ追加しました。データ参照・静的検査、build:scenarios、check:docs（50文書・2460ローカルリンク・13保存原文と集計の一致）、git diff --checkも成功しました。HTTPで `/adv/` 配下の225HTML、対応JSON、CSS・モジュール・リンク・Schema読込と全原稿の検証を確認しました。

実ブラウザの描画確認は未完了です。tools/browser-config-smoke.mjsはChromium実行ファイルがなく起動できませんでした。DOMテストとHTTP確認は、実際の画面寸法・描画・ネイティブのコピー操作を証明しません。再現スクリプトには一覧検索、詳細編集、全文コピー、不正値、再編集時の出力無効化、1280／390／320px幅の確認を用意しました。

ブラウザの検証範囲は形式・数値・このファイル内の整合です。別原稿・画像・生成後の到達可能性は、貼り付け後の再生成と既存検証で確認します。編集HTMLにはGitHub認証・API書込み・自動反映を実装していません。

## PR #38の便利調べる・任意調べる

便利調べるで未読・変化・有効操作を選別し、単独の無料操作は直接実行します。消費時は一つでも明示選択。任意調べるはセル・正面エッジ・配置物・装置を選び、既読情報や実行不能理由も再確認できます。RとUIボタンを追加し、既存のキー設定は衝突せず保持します。q001の壁灯3件と篝火の地下二・三層の壁面松明をエッジへ配置しました。

`npm run check` は全633テスト成功、失敗0・skip 0。追加21件で、未読・変更・無関係な変更、調査で新しく読める説明、状態変更後の消費分岐、単独無料・複数対象・消費時の選択、金・道具・MPの取消と確定、無効理由の再読、秘密と一回報酬、途中保存、古い座標・向きからの選択拒否、エッジの操作・表示・照明、ミニマップ、Rの変更・リピート・旧設定の保持を確認しました。既存の階段・q001の実到着・100依頼の通し経路も成功しています。

通しの `build:scenarios` と最終 `build:catalog`、データ参照・静的検査、JSON Schema Draft 2020-12による51文書（原稿13迷宮、data/maps内34文書（通常ロード33件を含む）、q001原稿とクエスト、迷宮一覧、manifest）の検査が成功しました。`check:docs` は49文書・2452ローカルリンク・13保存原文とデータ集計の一致を検査します。`git diff --check` も成功しました。変更したゲーム原稿は壁灯・松明5件のedge指定だけで、配置セル、人物、報酬、敵、戦闘数値、q001の油消費と本文は維持しています。

実ブラウザの確認は未完了です。`tools/browser-scene-smoke.mjs` はChromiumの実行ファイルがないため起動できませんでした。両Viewのキー操作・コマンド表示とミニマップはDOMテストで確認しましたが、実際の画面での寸法・描画・音声はこの結果に含めません。

スクリプト解析は読取専用で、未知の副作用・乱数・再帰・上限超過や書込み後の消費可能性は保守的に選択を残します。完全な無効果判定ではありません。新しい命令や装置を加える際は[調査仕様](ui/INSPECTION.md)の判定契約を更新します。

## PR #37の条件付きフィールドイベント

迷宮原稿に通知の購読・条件・再発・成立時処理を定義し、通常くらがり襲撃を火の専用dangerフックから移しました。現在の迷宮で購読する候補だけを評価し、照度・セル・固有環境の値は条件が参照したときに計算します。保留中の通知をまとめ、保存後も最新条件で定義順に再開します。戦闘開始時に古い環境battle候補を消費し、q001の物語戦闘と通常襲撃の重複を防ぎます。専用処理を維持した守護者と空気供給の理由はイベント一覧へ記録しました。

`npm run check` は全612テスト成功、失敗0・skip 0。追加10件は購読した候補だけの評価、通常の火と魔除けの違い、移動後の燃料切れ、物語戦闘優先、逃走後の即再戦防止、once／entry／change、保留と保存、条件の再評価、地点と照度、不正な定義・保存の拒否を検証します。q001の新人救出、油の消費、強制終了と現地会話、既存のセル進入と旅程も成功しました。データ参照・静的検査も成功しています。

最終確認で、同じフラグ値の再代入やスクリプトのlocal変数を環境変更として通知しないよう補正し、関連41テストが再度成功しました。`npm run build:scenarios` の通し再生成、Schema15文書（原稿13件・ダンジョン一覧・manifest）、`npm run check:docs`（48文書・2440ローカルリンク・13保存原文）、`git diff --check` も成功しました。33マップ・200クエスト・配布スクリプトは基準masterと差分なし。人物・道具・敵・遭遇の値も一致しています。

今回の実ブラウザ確認は未実施です。Coreの判定と表示用投影の照度一致を自動テストで検証しました。操作・画面配置の変更はありません。

## PR #36のセルレイヤー整理

全33マップの通行投影を維持しながら、セル種プリセットと地点上書きを正本へ追加しました。表示・遮光・素材画像・照度・通水・セルイベントを通行値から分離し、地形変更の原稿・生成器・Schema・ランタイム検証・保存・描画へ接続しました。ゲーム内容版は1.16.0です。既存マップの通行配置、クエスト本文、戦闘・アイテム数値は変更していません。

`npm run check` は602テストすべて成功し、データ参照・静的検査も通過しました。うち新規10テストは通行と遮光の独立、画像と照度の上書き、通水属性、毒床を例にしたスクリプト処理、会話／戦闘中の保存再開、地点別一回性と再入場、破壊壁・植物・地形切替、異常定義の拒否を対象にします。

JSON Schema Draft 2020-12で原稿・セル種・イベント・配布マップ・ダンジョン・manifestの51文書と、空でないイベント／スクリプト例を検証しました。`npm run build:scenarios` の通し再生成後、`npm run check:docs` は48文書・ローカルリンク・13保存原文とデータスナップショットの一致を確認しました。33マップから追加cellsを除いた全内容、200クエスト、人物の値、既存ダンジョン規則を起点masterと比較して一致しています。再生成で消えていた人物の透過立ち絵説明は生成器へ取り込み、元の本文を維持しました。`git diff --check` も成功しました。

実ブラウザ確認は未実施です。この環境にChromium実行ファイルがなく、Playwrightの導入も配布ZIPの取得エラーで失敗しました。過去の画面検証結果を今回の成功結果として扱いません。描画用スナップショット・遮光・画像参照・照度は自動テストで検証しています。

## 前回の高さ調整（1.15.0）

通常画面の下部領域を44%から28%へ下げ、添付画像の「足元・正面を調べる」ボタン付近へメッセージの上端を揃えました。小さな画面でも本文とページ操作を確保するため最低200px、高さ550px以下では従来の48%を使います。本文量で伸縮せず、既存の実寸ページ送りと右側のスクロールが新しい領域に追従します。CSSと関連docのみを変更し、作品版1.15.0とセーブ仕様は維持します。

tools/browser-scene-smoke.mjsで実Chromiumの1280×856・900×700・390×844・320×568・844×390、本文24pxを確認して成功しました。縮小後の固定高さ、本文の収まりとページ送り、18候補の方向選択・自動スクロール・決定、探索記号の位置を確認しました。npm run check:docsは47文書・2411ローカルリンク・13保存原文とデータ集計の一致を確認し、git diff --checkも成功しました。CSSだけの変更なのでゲームロジックの全テストは再実行していません。

## PR #32で実装した内容

背景内表示の下部を固定し、メッセージの上端を保険審査員の胸元付近へ揃えました。通常44%、低い横画面48%の高さを使い、短い本文でも縮めません。探索の「？」は背景内表示の49%位置へ上げ、固定枠の上に収めます。透過した本文領域は、実寸・フォント・改行を計測してページ分割します。最後のページまではゲームを進行させず、同じ本文から選択肢へ移る場合は最後のページを維持します。キャンセルは既存の安全な取消先を優先し、取消先のない会話では前ページへ戻ります。

右側の選択肢・コマンドは固定枠内でスクロールします。方向キーで選択した項目へ自動スクロールし、決定・キャンセルも共通操作を使います。町・探索・会話・戦闘中の発言に対応し、従来表示も保持しました。

シナリオのcast.displayとscene.cast命令で、位置・大きさ・左右反転・重なり・表示画像・登退場を指定できます。say.characterで発話者を識別し、位置を変えず最前面へ出します。演出は会話途中の保存再開に対応し、人物の物理的な所在は変更しません。カード表示も選択できます。仕様は[CHARACTER_STAGING.md](ui/CHARACTER_STAGING.md)です。

q001は入口・老人発見・救助・帰還報告・戦闘中の発言へ配置を適用しました。詰所では老人とルーキーを左、リネを右へ置きます。三人の透過立ち絵を追加し、カード用の既存肖像は保持しました。原稿・配布JSON・スキーマ・カタログと関連UI文書を更新しています。台詞を分けて命令位置が変わるため内容版は1.15.0とし、既存方針どおり旧内容版セーブは新規開始します。

## PR #32の検証

`npm run check` は全592テスト成功、失敗0、skip 0です。今回6件を追加し、演出と発話者の保存再開、choiceへの引き継ぎ、位置と表示の独立、投影の複製、配置・参照の検証、q001の原稿と配布JSON、前後関係と反転、カードの維持、Unicodeの分割と再配置後の読み位置を確認しました。探索記号の最終位置調整後には描画・画面の関連36テストと実ブラウザ検証を再実行し、成功しました。人物ID付き台詞を常体化しない検査も追加しています。

200クエスト・33マップ・2319スクリプトの参照、全画像、構文とView分離の静的検査、通常資源での100クエスト完走、全200クエストの結末探索も成功しました。

`npm run check:docs` は現行47 Markdown・2411ローカルリンク・13保存原文とデータ集計の一致を確認しました。`git diff --check` も成功しました。

tools/browser-scene-smoke.mjsをChromium 153と日本語フォントで実行し、成功しました。1280×856で審査員を基準とする枠高、発話者の前後関係、長文と改行の欠落がない分割、ページ送りでゲーム状態が変化しないこと、取消による前ページ移動、本文から選択肢への接続、18個の選択肢を方向キーで末尾まで選び決定できること、スクロール位置とフォーカス枠を確認しました。

900×700・390×844・320×568・844×390と本文24pxで、再分割、本文の収まり、横方向のはみ出しがないこと、末尾の選択肢までのキー操作を確認しました。従来表示の三人のカードと、探索の「？」が固定枠より上に表示されることも確認しました。検証中に見つかった文字サイズ変更後の再計算、人物ID付き台詞の文体処理、従来の安全な取消との互換性を修正しました。

## 実画面と残作業

上記の実ブラウザ検証はローカルの実装を使用しています。公開版への反映後の通し操作、すべての管理画面・キー設定の変更と再読込・音声・ゲーム全編の人物演出は今回の確認範囲に含めません。新しい透過立ち絵はルーキー・老人・リネを追加し、既存の審査員・学芸員・荷役人も使用します。他の人物は既存肖像へフォールバックします。

ゲームパッド・修飾キーの組み合わせ・タッチ専用配置・端末間のキー設定共有は未対応です。q004〜q200の全物語行動の物理接続、全200件の文芸監査、長期探索の調整、世界時計・一般NPCスケジュールも未完了です。次工程は[HANDOFF.md](HANDOFF.md)。

## 過去の検証記録

### PR #29：UI文書整理とキー設定

UI関連7文書をdoc/uiへ移し、根本方針と現状を文書化しました。記録のキー設定に主キー・副キー、競合検出、下書きの適用・破棄・初期化、ブラウザへの保存と復旧を追加しました。追加21件を含む全576テストとcheck:docsが成功しました。詳細は[キー設定仕様](ui/KEY_CONFIG.md)を参照してください。

PR #28の共通キー操作は全555テスト成功。追加23件で選択肢の初期フォーカス、戦闘対象、装備・転職、記録、広場・探索への取消を確認しました。

PR #27の背景内表示は全532テスト成功。切り替え、長文分割、画面内ウィンドウ、効果の配置と検索位置保持を検証しました。

PR #26の文書整理は全522テスト成功、q002専用ページ・6移動行為・3マップと全文を照合しました。PR #25のUI実装は全520テスト成功、PR #24の2D区画は全512テスト成功でした。版ごとの詳細・当時の制約・旧水量保存や移行の結果は[2026-09-18以前の記録](legacy/2026-09-18/PROGRESS.md)へ保持しています。1.3.0のバランス実測は[2026-09-14の保存資料](legacy/2026-09-14/README.md)です。古い計測値を現行版の実測として流用しません。
