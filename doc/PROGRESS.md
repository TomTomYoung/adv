# 検証結果と残作業

確認日: 2026-09-19。作品版1.14.0。基準masterは `5f029b8`（PR #26マージ済み）。背景内ビューの追加は `feat/in-scene-view` で行います。

## 今回の変更

背景内ビュー `SceneView` と従来の `GameView` を切り替える「記録 → 画面配置」を追加しました。初期値は背景内表示で、ブラウザ設定へ保存します。既存の会話・戦闘・道具・隊・掲示板・地図の描画を再利用し、背景内の下部操作と一覧ウィンドウに配置します。

長文の表示専用ページ送り、一覧中の背面inert・ゲームキー遮断・Escape、検索位置の保持、効果の画面内格納・解放、記録・ヘルプダイアログの位置合わせ、プレビューの画面配置切り替えを追加しました。Core、Applicationの投影、配布JSON、原稿、素材とセーブ形式に差分はありません。

## 検証

`npm run check` が成功しました。全532テスト、失敗0、skip 0。配布データ参照・静的検査・View分離も成功しました。通常資源での100クエスト完走と全200クエストの結末探索を含みます。

追加10件は11表示例を両ビューで描画し、選択肢の条件と敵対象を照合します。GameEngineと接続して切り替え・メニュー開閉のセーブ完全一致、キー遮断、長文を読み終わるまでadvance/chooseを送らないこと、同文の連続、掲示板から会話への移行、戦闘中会話、効果の格納・解放、検索フォーカス・カーソル・スクロール保持を確認しました。

`npm run check:docs` は現行42 Markdown、13保存原文のハッシュ、ローカルリンク、データ集計・専用ページ・配置図の一致を確認しました。`git diff --check` も成功しました。

今回の描画試験はNodeの小さなDOMアダプターです。CSSの寸法・描画・ブラウザのネイティブフォーカスは模擬せず、実画面が確認済みという意味ではありません。シナリオ生成器・配布データは変更していないため、今回の検証で再生成は行っていません。

## 実画面と残作業

2026-09-19の実ブラウザ接続は、ローカルプレビューへのアクセスが `ERR_BLOCKED_BY_CLIENT` で拒否されました。320〜1440px・低い横画面・本文24pxで、背景内の収まり、人物と敵の見切れ、全ボタンへの到達、長い選択肢と一覧のスクロール、実ブラウザでのフォーカス移動、記録・ヘルプダイアログ、暗闇時の文字、音声を確認する必要があります。手順は [IN_SCENE_VIEW.md](IN_SCENE_VIEW.md) と [HANDOFF.md](HANDOFF.md) に記載しました。

q004〜q200の全物語行動の物理接続、全200件の文芸監査、追加100本を含む長期探索の調整、世界時計・一般NPCスケジュールは未完了です。次工程は [HANDOFF.md](HANDOFF.md)。

## 過去の検証記録

PR #26の文書整理は全522テスト成功、q002専用ページ・6移動行為・3マップと全文を照合しました。PR #25のUI実装は全520テスト成功、PR #24の2D区画は全512テスト成功でした。版ごとの詳細・当時の制約・旧水量保存や移行の結果は[2026-09-18以前の記録](legacy/2026-09-18/PROGRESS.md)へ保持しています。1.3.0のバランス実測は[2026-09-14の保存資料](legacy/2026-09-14/README.md)です。古い計測値を現行版の実測として流用しません。
