# 検証結果と残作業

## 全滅時の区間巻き戻しと老人への再遭遇

2026-09-27、帰還封印の初回実装へ追加しました。既存の全滅時の町への帰還・救援費・回復処理を確認し、共通の `event.checkpoint.begin` / `event.checkpoint.commit` と全滅時の復元処理を追加しました。q001で救援前に全滅した場合は、老人との遭遇からの封印・帰路・人物所在・消灯済み記録・最後の壁灯を取り消し、老人セルへの再遭遇からやり直します。前便の「全滅後もq001の封印を維持する」仕様を置き換えています。救援成功後は確定し、以後の全滅では巻き戻しません。

`npm run check` は760試験成功、失敗0・skip 0です。追加9試験と既存の敗北試験の更新で、通常の全滅帰還、老人との会話中断後・帰路・くらがり戦闘での全滅、保存読込、町から実歩行での再遭遇、連続2回失敗、再挑戦時の再消灯、救援成功後の維持、致死トラップ、古い敗北継続の破棄、別クエストでの共通命令の再利用、所持品消費・救援費・別の罠の維持、不正な原稿と保存の拒否を確認しました。全体検査で見つかった命令探索ツールとのキー衝突は、復元対象の記録キーを `eventKeys` として解消しています。

`npm run check:docs`、原稿からのクエスト・スキーマ・専用ページ・カタログ生成、`git diff --check` も成功しています。今回の実ブラウザー描画・公開Pagesの通し操作は未確認です。[共通仕様](../scenarios/EVENT_CHECKPOINTS.md)、[回帰試験](../../tests/event-checkpoints.test.mjs)。

## 帰還印封印・帰還禁止の初回実装（全滅時仕様は上項で更新）

2026-09-27、master `3cff23e` を基点に作品版1.22.0へ更新しました。トラップ・イベント用の設定／解除命令、迷宮・禁止対象・発生元別の保存、Coreの帰還拒否、両ビューの封印ボタン・理由表示を追加しました。q001は老人の発見直後に帰還禁止を付与し、新人救援によるイベント戦闘終了後、現地の救助会話へ進む前にその封印だけを解除します。

`npm run check` は751試験成功、失敗0・skip 0です。追加9試験で老人遭遇・会話中断・帰路・戦闘・新人発言・戦闘終了の境界、保存読込、早期勝利／逃走／撃退、通常戦闘、全滅救助後の再挑戦、別の罠との重複、実際のセル進入トラップによる共通DSLの発火、入口・直接操作・スクリプト・古い確認画面からの迂回防止、不正保存／原稿の拒否、封印中の無消費と解除後の帰還費を確認しました。両ビューのボタン無効化・理由表示はDOM試験です。

原稿からクエスト・スキーマ・専用ページ・カタログを再生成しました。人物JSONの生成によるキー順だけの差分は全値一致を確認して元へ戻しています。`npm run check:docs` は62現行文書・2758内部リンク・20保存原文とデータ指紋の一致、`git diff --check` も成功しました。今回の実ブラウザー描画と公開Pagesの通し操作は未確認です。旧版セーブは既存方針どおり移行しません。[封印・禁止の仕様](../dungeons/DUNGEON_RESTRICTIONS.md)。

確認日: 2026-09-26。作品版1.21.0。最新作業はmaster `4cc36b5`（PR #49反映済み）を基点とした水路拡張・地下関所・q004の実移動です。過去の検証は各節の日付と条件を参照してください。

## 地下水道の拡張・詰所・q004の実移動

2026-09-26、地下水道10区画を拡張し、浅い水張り76・深い水張り69・底なし水張り23セルを配置しました。浅い床だけ通行でき、既存の扉・操作盤・階段・現地イベントへの経路を保持しています。荷揚げ場の戸口から明示入室する詰所の背景を生成し、受付と留置室前を町と共通の表示へ接続しました。探索状態を保った退出と室内保存に対応します。[水路の仕様](../dungeons/WATERWAY_EXPANSION.md)。

q004は14場面・9移動へ分割し、地下の受付・留置室と地上の審査所を往復します。情報取得、罰金、申告、審査官の同行、仮証と釈放、姉妹の帰路を現地で確定します。3結末を維持し、強行救出の逃走・敗北後の現場再開も全シナリオ探索試験で確認しました。[個別経路](../scenarios/Q004_WORLD_ROUTE.md)。旧内容版は移行せず、作品版1.21.0で新規開始します。

`npm run check` は742試験成功、失敗0・skip 0。追加8試験は3種の通行、厳密な戸口と明示入室、探索番号・排水・灯油の保持、室内保存、不正な出入口と保存の拒否、3結末、二つのビューの退出・取消を検査しました。旧3D専用の試験は現行の新規室内を混入させず、退避原稿のハッシュを維持して実行しています。

生成背景と、実際の投影・描画処理で生成した[水路の描画例](../dungeons/screenshots/waterway-2026-09-26.webp)を目視確認しました。実ブラウザーでの画面配置・クリック・公開Pagesは未確認です。DOM模擬試験とソフトウェアCanvasの結果を実機確認とは扱いません。英日プロンプト・生成元と配布画像のSHA-256は[制作記録](../../assets/source/world-locations/checkpoint-2026-09-26.json)に保存しています。

## くぼみ6種類と境界・水面の描画

2026-09-26、浅い・深い・底の見えないくぼみと各水入りをセル種へ登録し、同深度の内部側面を省略する床面投影を追加しました。編集パレット、深さ・水面高さの小数入力、隣接縁プレビュー、ミニマップ、独立した表示確認ページも追加しました。[仕様](../dungeons/FLOOR_DEPRESSIONS.md)。

6種類×単独・2セル・2×2・L字・深さ混在×4方向の120フレームを、実際の投影・描画コードとSkiaのCanvas互換環境で生成しました。6種類の比較画像と混在例を目視確認しています。ブラウザーの取得はアーカイブ破損で失敗したため、これは実ブラウザーでのレイアウト検証ではありません。公開Pagesと実ブラウザーでの表示・操作は未確認です。

`npm run check` は734試験成功、失敗0・skip 0です。追加11試験で6種の投影・通行・保存、同深度の境界省略、異なる深さ・底のない側面、4方向・L字・斜め接触、閉じた境界による遮蔽、水面の前後関係、編集画面の小数と負値の入力、不正水位の出力拒否を確認しました。`npm run check:docs` と `git diff --check` も成功しました。描画比較は[6種類の比較画像](../dungeons/screenshots/depressions-2026-09-26.webp)です。

## パーティーと酒場の並び替え

2026-09-26、待機／加入の右に△・▽を横並びで追加しました。選択人物を表示一覧内で一つ上／下へ移し、人物と詳細ページを選択したままにします。端に着いて操作ボタンが無効になった場合は、その人物のカードへフォーカスを戻します。パーティーはmembersの実隊列、酒場はrosterOrderの保存順を使います。酒場内の移動は在隊者を飛ばし、パーティーの順番を変更しません。再加入・再待機でも酒場の保存順を保持します。

`npm run check` は723試験成功、失敗0・skip 0です。追加6試験で実隊列と各カード一覧への反映、酒場の独立した順序、加入・待機後の保持、記録・再読込、既存の同版記録、不正な順序の拒否、端・対象違い・会話・戦闘・探索中の拒否、両ビューのボタン配置と選択・フォーカスの維持を確認しました。HP・MP・所持品・ゴールド・歩数・乱数は並べ替えで変わりません。

`npm run check:docs` と `git diff --check` も成功しました。実ブラウザーでの描画確認は今回実施していません。[人物画面](../ui/CHARACTER_PROFILES.md)、[並び順の試験](../../tests/party-order.test.mjs)。

## ショップ・人物プロフィール・隊の状態の再構成

2026-09-26、ショップを品物名＋価格の項目一覧、選択した品の能力値・説明、常時見える下部のキャラクターカードへ変更しました。カードには装備可否・個人所持数・装備中の個数を表示します。商品決定は購入先へフォーカスし、カード確定でだけ1個購入します。

町の隊カードから選択した人物のプロフィールを開き、転職・スキル・魔法・所持アイテムへ切り替えます。隊の状態は左上のパーティー選択と右上の詳細、左下の酒場選択と右下の詳細です。各一覧は画像・名前・HP・MPのカードと待機／加入の1ボタンに絞り、上下の選択・詳細ページは独立しています。概要には転職・魔法使用・装備解除・休息を並べません。探索特技の使用はスキル・魔法ページへ移しました。所持・購入・転職・編成のCore判定と保存形式は維持しています。

`npm run check` は717試験成功、失敗0・skip 0です。追加7試験で両ビューの選択人物、プロフィールのページ切替と無消費、上下独立の人物選択、選択人物への加入・待機、一覧のスクロール位置保持、ショップ一覧と購入先の同時存在、所持品／装備中の個数更新、投影の状態非破壊を確認しました。既存の転職キー操作と取消、サイド欄のクエスト案内の試験を新しい入口に更新しました。全体検証にはデータ参照・静的検査・225設定原稿とHTMLの一致も含みます。

`npm run check:docs` は58現行文書・内部リンク・20保存原文のハッシュと生成文書の一致を確認しました。`git diff --check` も成功しました。Chromiumの取得に失敗し、利用可能な実ブラウザー接続もなかったため、今回のレイアウトの実寸・狭い画面・公開Pagesの目視確認は未実施です。DOM試験を描画確認の代用として扱いません。旧ショップの前便のスクリーンショットは今回の表示証跡ではありません。[人物画面](../ui/CHARACTER_PROFILES.md)、[ショップ](../ui/SHOP_AND_PREPARATIONS.md)。

## 9月25日の文書整理の確認

`npm run build:scenarios` と `npm run build:catalog` が成功しました。全体生成では人物JSONのキー順だけが変わりましたが、JSONの全値が一致することを確認し、元の並びへ戻しました。ゲーム原稿・配布JSON・srcは基点と差分がありません。

`npm run check` は693試験成功、失敗0・skip 0です。200クエスト・33マップ・2320スクリプトの参照と静的検査、225設定原稿・HTMLの生成一致を確認しました。文書生成先の回帰試験では新しい全分野の配置、同じパスを二度処理した場合の安定性、分野をまたぐリンクと素材参照を検査しました。

`npm run check:docs` は現行55文書、内部リンク・画像・見出し・索引、20件の保存原文ハッシュ、配布データの集計・指紋、人物画像・クエスト専用ページ・配置図の生成一致を確認しました。`git diff --check` も成功しています。

実ブラウザ・公開Pagesの再確認、長期バランスの再測定は今回実施していません。編集画面の変更は「使い方」のリンク先のみです。

## 同日の魔物カタログ追加修正

初回整理 `f5167dd` 後に残っていた旧本文とAIの表示漏れを修正しました。`npm run build:docs`、`npm run check:docs`、`node --test tests/monster-catalog.test.mjs` の3試験、`git diff --check` が成功しました。現行55文書、2598件の内部リンク、20件の保存原文と生成一致を確認しています。全体693試験の結果は上記の初回整理時のもので、この追加修正では再実行していません。ゲームの原稿・配布JSON・srcに差分はありません。

## 旧共通魔物素材・第1便

2026-09-25、スライム・スケルトン・構造体の3点を内蔵画像生成で作成し、背景透過のWebPへ換装しました。画像IDを共有する計20定義の敵に反映されます。旧PNGは元コミットとバイト一致し、AIPaint原稿も保持しています。生成PNGからWebPへの変換では画像寸法とアルファ値の全画素一致を確認しました。生成条件・ハッシュ・透過画素数は `assets/source/monsters/refresh-2026-09-25-batch1.json` と同名の `-prompts.json` に記録しています。

`buildWorld` による正本からの再生成、`npm run build:docs`、`npm run check:docs`（55文書・2601リンク・20保存原文）、`node tools/validate.mjs`、`node tools/check-static.mjs`、`git diff --check` が成功しました。ゲームデータの変更は `data/assets.json` の3画像参照のみです。公開Pagesの実画面は今回再確認していません。第1便終了時点では旧共通画像のドラゴン1点が残っていました。

## 旧共通魔物素材・第2便

2026-09-25、第1便で残ったドラゴン1点を内蔵画像生成で作成し、透過WebPへ換装しました。共通画像IDを共有する7定義へ反映済みです。現行59定義が使う25画像ファイルはすべてWebPとなり、旧共通PNGを使う敵は0定義です。旧PNGは元コミットとバイト一致し、AIPaint原稿も保持しています。画像寸法・アルファ値を維持した変換と、全身が画像内に収まることを確認しました。生成条件・ハッシュ・透過画素数は `assets/source/monsters/refresh-2026-09-25-batch2.json` と同名の `-prompts.json` に記録しています。

`buildWorld` による再生成、`npm run build:docs`、`npm run check:docs`（55文書・2605リンク・20保存原文）、`node tools/validate.mjs`、`node tools/check-static.mjs`、`git diff --check` が成功しました。ゲームデータの変更は `data/assets.json` のドラゴン画像参照のみです。公開Pagesの実画面は今回再確認していません。

## ミニマップの正方形素材・暗所視認性・拡大表示

2026-09-25、地図の文字表示を24×24のSVG素材18点へ置き換えました。セルは正方形に固定し、現在地の画像だけを回転します。探索済みの暗所は地形画像の明度85%を下限とし、未探索の地形・物体・照明は引き続き隠します。両方の画面形式に「拡大」「元に戻す」とキャンセル操作を追加しました。拡大中のゲーム入力を止め、地図を閉じると呼び出し元へフォーカスを戻します。

`npm run check` は700試験成功、失敗0・skip 0です。追加した4試験は18素材の寸法・4方向の画像回転・暗所と未探索の区別・両画面の拡大と復帰・ゲーム状態と入力の保持を検査します。`npm run check:docs`（56文書・2618リンク・20保存原文）と `git diff --check` も成功しました。全体検査で前便の魔物画像参照に未生成の設定キャッシュが見つかったため、`npm run build:config` で正本と一致させています。

実装コミット `2906e12` はmasterへ反映済みです。GitHub Actionsの「Validate dungeon RPG」と「pages build and deployment」は両方成功しています。ローカル画面は確認用ブラウザーの接続制約により開けませんでした。公開Pagesでは1363×936の画面で迷宮へ入り、配信完了後にも再読込みしましたが、確認環境には旧版の文字地図が残っていました。新しい表示の実寸・公開画面での拡大操作・狭い画面は未確認であり、自動試験の成功と区別します。素材と表示仕様は[ミニマップ](../ui/MINIMAP.md)を参照してください。

## ショップと旅支度の分離

2026-09-26、ショップの独立画面・所持金の固定表示・顔画像付き購入先選択を追加しました。装備可否を職業ごとに表示し、決定した仲間へ購入品を渡します。旅支度は共通の袋と個人の持ち物を切り替え、説明・使用・1個または全部の受け渡し・装備・解除を扱います。装備できない品物の持ち運びも可能です。個人の所在と既存の隊全体所持数を整合させ、装備の個体ID・腐食状態とセーブを保持します。

`npm run check` は710試験成功、失敗0・skip 0です。専用10試験では購入条件・決定時だけの消費・取消・使用元・直接在庫を書き換える調合との整合・装備個体・離隊時の回収・保存と不正データ拒否・会話や戦闘中の制限・両ビューの操作を確認しました。既存の標準キーと変更済みキーによる相手選択・取消の試験も更新しています。`npm run check:docs` と `git diff --check` が成功しました。内容データ・価格・職業の装備制限は変更していません。

実装コミット `bf522b5` はmasterへ反映し、GitHub Actionsの全体検証とPages配信が成功しました。公開版を1363×936のブラウザーで操作し、所持金の固定表示、顔画像4人の表示、装備可否、取消時の110G維持、ニオへの鉄の剣購入後の15G、ニオからアダへの受け渡し、アダの装備・解除、再読み込み後の保持者と残金を確認しました。購入先選択画面に横方向のはみ出しはありません。証跡は[購入先選択](../ui/screenshots/shop-recipient-2026-09-26.jpg)と[旅支度](../ui/screenshots/preparations-2026-09-26.jpg)です。実画面確認は背景内表示・上記の画面幅で行い、従来表示と狭い画面の描画は今回再確認していません。両ビューの操作はDOM試験で確認しています。[現行仕様](../ui/SHOP_AND_PREPARATIONS.md)を参照してください。

## 過去に確認済みの範囲

2026-09-24のマップ編集8タブと共有エッジの実装では `npm run build:scenarios`、`npm run check` の692試験、`npm run check:docs` が成功した記録があります。ローカルChromiumでmap・cell・configの各smokeスクリプトを実行し、共有エッジ、サイズ変更、24セル種、接続先作成、複数原稿出力・コピー、1082×604と1440／900／390／320px幅を確認しています。これらは今回のブラウザ再実行結果ではありません。

戦闘UIは過去のローカルChromiumで階層選択・対象取消・無消費・一回の道具消費と回復・次手番フォーカス、候補多数時の内部スクロールを確認済みです。会話画面は固定枠、実寸ページ分割、18選択肢、人物配置、従来カードを確認した記録があります。全戦闘・全場面を通した検証ではありません。

素材第10便でくらがり1点・人物30点の計31点を適用済みです。原記録は画像ハッシュ・アルファ・参照・原稿とdataの一致を確認しています。公開画面での全人物確認は未実施です。

各作業の実施日・条件・制約・テスト件数は[旧PROGRESS](../legacy/2026-09-25/PROGRESS.md)と[旧HANDOFF](../legacy/2026-09-25/HANDOFF.md)へ原文保存しました。さらに古い結果は[履歴索引](../legacy/README.md)にあります。

## 未確認・未完了

1. 公開Pagesでの通し操作、全管理画面とキー設定の全経路、全戦闘、音声と全シナリオの人物演出。
2. q004〜q200の全物語行動の物理移動への接続。q030までの2Dマップ接続済みという事実と区別します。
3. 全200件の文芸監査、長期探索の距離・HP・MP・材料・帰還費の調整。1.3.0の旧バランス測定を現行版の実測にしません。
4. 世界時計・一般NPCスケジュール、ゲームパッド、タッチ専用配置、修飾キーの組み合わせ、キー設定の端末間共有。

編集元と次工程は[HANDOFF.md](../HANDOFF.md)、長期測定の条件は[BALANCE_PLAN.md](../battle/BALANCE_PLAN.md)を参照してください。
