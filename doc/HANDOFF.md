# 実装引き継ぎ

確認日: 2026-09-23。作品版1.18.0。起点はPR #39まで反映済みのmaster `79f43e3`。作業ブランチは `feat/battle-command-layout`。戦闘コマンドの階層化、左下コマンド・右メッセージ、固定高の内部スクロールを実装しました。ゲーム配布データ・内容版・保存仕様は変更していません。

## 2026-09-24：たいまつと魔物の出現仕様の確認

[篝火の仕様書](KAGARIBI_DUNGEON.md#2026-09-24の仕様整理通常のたいまつと魔物除け)へ、今回の指定と現行実装の違いを追記しました。普通のたいまつの点灯中も通常の魔物は出現し、完全な魔物除けはその効果を持つ特殊な火に限定します。今回は調査・文書更新のみで、ゲームコード・設定・配布データは変更していません。

現行の普通の火はrepels:falseのため、明るくても守護する壁灯などの範囲外でくらがりに襲われます。入口のwardはrepels:trueかつ通常遭遇倍率0です。複数の火の通常遭遇倍率は優先度で決まります。普通の火によるくらがり防護の変更と、特殊火同士が重なる場合の完全抑制の優先順位は未実装・未確定として区別しています。

q001は携行火を入口25歩・老人8歩へ上書きし、帰路の消灯地点で0にした後に物語戦闘を開始します。途中の燃料補充もこの上書きの対象です。報告された遭遇がこの経路か通常襲撃かはユーザーのセーブを再現しておらず未確認です。master e83d750の火部品・迷宮原稿・q001原稿をソース確認しました。文書だけの変更のためゲームの再生成・動作テストは実施していません。

## 今回の戦闘UI

左下には行動者と「攻撃・防御・スキル・アイテム・逃走」を表示し、スキル／アイテムの具体的な候補と対象選択は同じ枠を切り替えます。キャンセルは対象から元の候補、候補から最上位へ戻してフォーカスを復帰します。未確定の階層と行動はView状態で、Coreへの送信は実行時だけです。手番の交代・戦闘イベントへの移行・戦闘終了で未確定状態を解除します。

背景内ビューの下部高さは28%・最低200px、低い横画面48%を維持しました。左右の枠高を揃え、行動者を残して候補一覧だけを縦スクロールします。右に戦闘ログ・解析結果を置き、常設キー説明・英語のラウンド見出し・使用不能理由の一括表示・探索用装置summaryをコマンドから外しました。戦闘中会話も左操作・右本文です。従来ビューは同じ階層と左右配置を240pxの固定パネル内に描画します。

`tools/browser-battle-smoke.mjs` が再現用です。実GameEngineの技能・道具・取消・確定消費と、24件の仮候補によるスクロール負荷確認を区別しています。ローカルChromiumで5画面寸法の左右位置・固定高・末尾へのキー到達を確認しました。既存の `tools/browser-scene-smoke.mjs` による通常会話・人物・ページ送りの検証も成功しています。公開版の通し操作と音声は未確認です。検証件数は[PROGRESS.md](PROGRESS.md)、配置は[IN_SCENE_VIEW.md](ui/IN_SCENE_VIEW.md)、操作は[KEYBOARD_CONTROLS.md](ui/KEYBOARD_CONTROLS.md)を参照してください。

## 現在の構成

[CURRENT_STATUS.md](CURRENT_STATUS.md)を現在の反映状態、[SPEC.md](SPEC.md)を共通仕様、[PROGRESS.md](PROGRESS.md)を検証結果の入口にします。版ごとの追記が積み重なっていた旧引き継ぎ・進捗・監査は[2026-09-18の保存原文](legacy/2026-09-18/README.md)へ分離しました。

シナリオ・クエスト系文書は [scenarios/README.md](scenarios/README.md) から参照します。q001・q002の全文は専用ページ、q003〜q020の全文はカタログ、q021〜q200は概要と分岐・結末一覧です。`doc/QUEST_CATALOG.md` など旧配置へ再出力しないでください。画像も `doc/scenarios/quest-maps/` に置きます。

## 正本と生成

1. q001は `authoring/story-q001.mjs`、q002は `authoring/story-q002.mjs`、q003〜q010は `authoring/stories-v11-1.mjs` と `stories-v11-2.mjs`。人物は `authoring/characters.mjs`、共通の状態記法は `authoring/story-kit.mjs`。
2. q011〜q020は `config/catalog-q011-q020.json` と対応する生成処理。q021以降は `authoring/structures-*.mjs`、`scenarios-*.mjs`。個別の構造を共通テンプレートへ揃えないでください。
3. 現地イベントは `config/quests/qXXX.events.json`。マップは `config/connected-maps.json`、篝火は `config/kagaribi-content.json`、装置・接続は `config/dungeons/*.json`。町は `config/locations.json`。
4. 生成済みのdataだけを直さず、原稿を変更して `npm run build:scenarios` を実行します。文書だけなら `npm run build:catalog` でカタログ・専用ページ・図を、`npm run build:docs` で各一覧と集計を更新します。
5. `tools/doc-layout.mjs` が生成先とリンクの相対位置を一元化します。生成器内のMarkdownは従来のdoc直下基準で組み立て、ファイルへ書く際だけ配置に合わせます。`check:docs` はサブフォルダを再帰走査し、各フォルダの索引、参照、生成結果を照合します。

ユーザーがカタログ・専用ページを改稿した場合は、先にその内容を原稿へ取り込んでから再生成します。本文の編集を機械的な再生成で消さないでください。

## 維持する仕様

人物演出の仕様は[CHARACTER_STAGING.md](ui/CHARACTER_STAGING.md)です。原稿のstory.scenes.cast.display、またはscene.cast/scene.cast.clear命令で配置し、say.characterで発話者を指定します。物語上の所在と表示の上書きを混同しないでください。presentation.castとspeakerIdは保存対象、本文ページ番号・フォーカス・スクロールはView状態です。発話者の最前面化は座標を変えません。q001の帰還報告では老人とルーキーを左、リネを右に保持します。新規透過素材のプロンプト・ハッシュはassets/source/characters/dialogue-sprites-*.jsonに記録しています。

下部枠は内容量で伸縮させません。scene-view.jsがフォントと実際の本文領域を計測してページを作り、文字サイズ・画面寸法変更後も読み位置を保持します。ResizeObserverは本文要素も監視し、フォント変更だけの再計算を落とさないようにしています。本文をchoiceへ引き継ぐ際に先頭へ戻さないでください。安全な取消先は前ページ移動より優先します。方向キーによる選択とscrollIntoViewの連動、カード表示への切り替えを保持します。

UIの判断基準は[PHILOSOPHY_AND_STATUS.md](ui/PHILOSOPHY_AND_STATUS.md)、文書の入口は[ui/README.md](ui/README.md)です。画面・入力・メッセージ・表示契約・効果とSEの文書をdoc直下へ戻さないでください。`tools/doc-layout.mjs` のuiDocsが生成先を持ち、`check:docs` が旧位置への再出力と索引・参照を検出します。保存原文は今回移動・編集していません。

キー設定は[key-config仕様](ui/KEY_CONFIG.md)を参照してください。`key-bindings.js` は論理操作と物理キー、検査、保存形式の正規化を担当し、`key-config.js` が下書きを編集します。main.jsはsettings.keyBindingsへ保存できた場合だけ現在設定を更新します。`keyboard.js`と`system-controls.js`は同じ割り当てを使います。入力待ちをゲームへ流さず、旧Enter/Spaceのネイティブ決定、文字入力中の文字キー、重複、必須キーの全解除を区別してください。EscとTabの回復経路を維持します。

[KEYBOARD_CONTROLS.md](ui/KEYBOARD_CONTROLS.md)が操作仕様です。`keyboard.js` は共通キーを現在の画面に応じて振り分けます。`view.explorationInput()`が真なら方向入力は直接移動、決定は調査です。それ以外はボタン選択・実行です。会話や選択肢が消えたら「調べる」を初期フォーカスにし、記録・ヘルプを閉じた際もmain.jsからresumeExplorationを呼びます。Tabによる明示的なボタン移動は補助経路とし、方向入力・キャンセル・再描画で探索へ戻します。`focus.js` が有効なボタンへの移動、選択欄の候補一覧、詳細の保持を担当します。`system-controls.js` は記録・ヘルプと新規開始確認の戻り順序を管理します。戦闘の未確定行動と対象はView状態です。選択中にCoreへ送らず、確定時に既存のbattle intentを送ります。キャンセルで逃走・有料帰還・必須判断を代行しないでください。

初期ビューは `SceneView`、従来ビューは `GameView` です。「記録 → 画面配置」で切り替え、settings.viewLayoutへ保存します。`src/view/view-layout.js` が差し替え、同じViewModel・dispatch・セーブを使います。`SceneView` は会話・戦闘・道具・隊・掲示板・地図の既存描画を再利用します。入力の振り分けは `src/view/keyboard.js`、長文分割は `message-pages.js`、背景内配置は `scene-style.css`。描画用ウィンドウの開閉・ページ送りをCoreの進行へ混ぜないでください。[仕様と検証範囲](ui/IN_SCENE_VIEW.md)を参照してください。

システムの記録・ヘルプダイアログは `main.js` で背景枠に合わせ、画面変更・リサイズ・スクロール時に位置を更新します。新ビューの効果は背景枠で切り取り、UIより下へ描きます。切り替え時は効果のタイマー・アニメーション・監視を解放し、同じfeedbackを再生しません。プレビューにも画面配置の選択を追加しました。

シナリオの本文・選択肢は同じメッセージウィンドウ。移動・調べる・帰還はプレイヤーコマンド。固有システムの常設操作パネル、独立した帰還モーダル、目的地で続きを進めるボタンを復活させないでください。Coreは `src/core/player-commands.js` で対象と条件を再判定し、表示・取消だけでは資源を消費しません。

セル進入・実到着の自動処理は `src/core/field-events.js`。q001は enter、q002の初回現地会話は interact、移動行為の帰着は自動です。足元と正面から調べられることを、正面を到着扱いできることと混同しないでください。

通常ロードは2Dマップ33件。地下水道の水路は完全水没時に水密扉で閉じ、乾いた隣区画の操作盤から排水します。単一セルの水壁、上下開口の直上だけに留まる水、完全水没区画への階段接続は認めません。旧3Dの原稿と実装は削除せず `authoring/legacy/2026-09-18-map-layout` と専用試験に保持します。

q001〜q003の物理移動は実装済み、q004〜q200は未完了です。シナリオ内で転移が必要な場合を除き、探索で現地へ到達させます。全200件の地の文は常体、台詞は人物の口調を維持します。内容版の異なるセーブは移行しません。

## PR #36で完了したセルレイヤー

通行値 `.` / `#` と、壁・床・遮光・素材・局所照度・通水属性・セルイベント参照を分離しました。[CELL_LAYERS.md](CELL_LAYERS.md) が実装仕様です。セル種は頻出構成のプリセット、例外地点は項目上書きです。`config/cell-layers.json` がセルの正本で、既存マップ原稿のtilesは最終生成でこの原稿の通行投影へ置き換わります。配置物と接続口は元の原稿で管理し、セル位置の変更時には双方の整合を検証してください。

破壊壁・植物・封印・取引・浮上・動力扉・地形切替は変更後のレイヤーを原稿へ明示しました。セルイベントは通常スクリプトへ接続し、同じ入場内の重複防止、地点単位の一回性、会話・戦闘中の保存再開を備えます。毒床の動作はテストで検証し、本編には新たな危険床を追加していません。旧3D・潮汐原稿は変更せず、専用の互換アダプターで保持しています。

602テスト・静的検査・Schema51文書・文書検査は成功しました。実ブラウザはChromiumを取得できず未確認です。[PROGRESS.md](PROGRESS.md)に範囲を記録しています。

`CELL_CATALOG.md` と `MAP_CELLS_AND_BOUNDARIES.md` もレイヤー基準へ改稿しました。生成物だけで修正しないでください。内容版が変わるため1.15.0以前のセーブは移行しません。

## PR #37で完了した条件付きフィールドイベント

`config/dungeons/kagaribi.json` のfieldEventsへ通常のくらがり襲撃を移しました。`fire_network.danger` は削除し、火の部品は保護状態・遭遇倍率・強度・燃料などを返します。明るさと魔除けは別です。通常松明は照度8でも魔除けがなければ襲われる仕様を維持しました。共通条件では `field.cell.parameters`、`field.illumination`、`field.environment.<system ID>` を使えます。[イベント仕様](scenarios/EVENT_SYSTEM.md)が記法と保存契約です。

現在の迷宮と購読通知で候補を絞り、進入・移動完了・灯火・物体・命令での状態変更時だけ追加します。保留中の重複通知はまとめ、会話・戦闘が終わったときに最新条件を評価します。once／entry／changeの再発、遭遇IDを指定するbattleと通常scriptの2種類の処理を備えます。新しい変更経路を実装するときは `signalFieldChange` を対応する箇所から呼んでください。条件が参照する値を変えても通知を出さない経路は、自動で監視しません。毎フレーム全イベントを走査する方式へ戻さないでください。

戦闘開始時には保留していた環境battle候補を消費し、同じ歩行からの連戦を防ぎます。次の移動や灯火変更で再評価します。q001の消灯・新人の戦闘内発言・強制終了・油の消費・現地会話は維持しました。保留候補・入場内の実行済み・回数を保存し、同版の会話／戦闘途中から再開できます。内容版1.16.0以前の保存は移行しません。

旅程到着は既存の共通arriveEventにあり、重複登録していません。power_gridの守護者は装置IDと勝利時状態までの一体管理、air_supplyはHP更新直後の全滅処理という理由で専用処理を維持します。[イベント一覧](scenarios/EVENT_CATALOG.md)に例外理由を記録しました。新しい危険処理はまずfieldEventsで表現できるか検討します。

612テストと静的・データ検査が成功しました。新規10件で購読限定、3種類の再発、照度と魔除け、移動後の消灯、保留の保存、最新条件、物語戦闘との競合、逃走後の即再戦防止、不正な定義と保存を確認しています。生成・Schema・文書検査の詳細は[PROGRESS.md](PROGRESS.md)。今回の実ブラウザ確認は未実施です。

## PR #38で完了した調査操作

ユーザーは「消費する場合は選択。他おっけー」と回答済みです。便利調べるをEnter・Space・E、任意調べるをRとUIボタンにし、キー変更にも対応しました。未読情報・変化・有効な操作を便利の候補にし、再読や実行不能理由の再確認は任意から扱います。対象が一つ、かつ無料の操作が一つなら直接実行し、消費または複数操作があれば選択します。候補ゼロでは無反応です。この判断を再確認する必要はありません。

[INSPECTION.md](ui/INSPECTION.md)が仕様と保存契約です。足元の篝火と向いているエッジのたいまつを同列に収集します。q001の壁灯3件と地下二・三層の壁面松明にedgeを追加しました。照明・魔除けは向きで消えません。スクリプトの解析で状態を変更せず、未知の副作用や状態変更後の分岐は保守的に選択を要求します。新規命令や装置は有効な変化・消費判定も更新してください。

同版の既読情報・途中の調査と選択を保存できます。旧内容版の保存は移行しません。既存のキー設定でRが使用済みなら維持し、任意調べるだけ未設定になります。検証結果と実ブラウザの未確認範囲は[PROGRESS.md](PROGRESS.md)に記録します。

## PR #39で完了した設定JSON編集

[設定一覧](../config/index.html)から225件を検索し、個別HTMLで対応するJSONを編集できます。対象はクエスト配置200件、迷宮13件、直下12件です。原稿はすべてconfigを正本とし、authoringに重複配置しません。生成器・テスト・現行文書の参照も移しました。旧原文・互換保存・素材制作記録は移動しません。

詳細フォーム、型ごとの入力、追加・削除・配列並べ替え、全体／部分JSON編集、ファイル読込、30回のUndo、検証後の全JSONコピーを備えます。文書全体を保持し、未表示項目を落としません。出力は2空白・LF・末尾改行一つで安定させます。入力エラー、重複キー、不正な数値、検証失敗、検証・読込中の再編集に対応しました。

[CONFIG_EDITORS.md](CONFIG_EDITORS.md)が対象・原稿の優先順位・検証範囲・使い方です。ブラウザでは形式・数値・ファイル内の整合を検査します。ファイル間の参照・素材・生成後の到達は、通常の再生成とプロジェクト検証で確認します。JavaScript原稿や生成済みdataを直接編集する画面は作りません。

github.devを開くこと、全文貼り付け、ブランチ作成、commit、push、PR作成は編集ツールの利用者が行います。HTMLにGitHub認証・API書込み・自動操作を追加しないでください。HTMLと一覧はbuild:configで生成し、check:configで全原稿との対応・検証・出力安定性を確認します。

実ブラウザの画面検証は今回未完了です。Chromiumが利用できないため、DOM操作とHTTP配信の確認範囲はPROGRESSに明記します。次に環境が整った際は設定一覧・詳細・コピー・スマートフォン幅も確認してください。

## 次の作業

1. 前回はローカルChromiumで固定枠・実寸ページ分割・18選択肢・共通キー・三人の配置と従来カードを確認済みです。1280×856、900×700、390×844、320×568、844×390と本文24pxを含みます。再現用はtools/browser-scene-smoke.mjsです。公開反映後の通し操作、全管理画面とキー変更の入力待ち・取消・適用・初期値復帰・再読み込み後の保持、音声は引き続き確認してください。全編の場面に必要な透過spriteと配置はシナリオごとに追加し、関係性を無視した一律配置へ置き換えないでください。
2. q004から物理的な場所・人物と物品の所在・出発・到着を個別に設計します。費用・戦闘・救助・証言は到着前に成立させません。
3. 全200本の文芸監査と、追加100本を含む長期探索の資源・距離・消耗調整を続けます。全世界の時計・NPCスケジュールは未実装です。

新しい変更では `npm run check:docs`、データ参照・静的検査、変更に対応するテストを実行し、生成先・リンク・ゲームデータの不要な差分を確認します。今回の具体的な検証は [PROGRESS.md](PROGRESS.md) に記録します。
