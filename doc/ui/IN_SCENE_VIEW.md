# 背景内ビューと従来ビュー

更新日: 2026-09-19。表示の追加はPR #27で反映済み。共通キー操作の実装起点はmaster `5eae7c1`。

## 切り替えと責務

「記録 → 画面配置」で背景内表示（scene）と従来表示（classic）を切り替えます。初期値は背景内表示。設定はブラウザのsettingsへ保存し、ゲームの記録ファイル・進行・乱数・判定には含めません。従来のGameViewを残し、新しいSceneViewは同じViewModelとdispatchを使います。core・シナリオJSONは変更しません。Applicationが既存の取消先と道具の対象種別を明示します。

町の背景または探索・戦闘の描画を一つのゲーム画面に広げ、上に表示情報を置きます。右上の手帳・道具・隊・記録、左上の現在地、下部のメッセージとコマンドは画面内に収めます。隊はHP等の短い表示、地図は探索中だけの小表示にします。本文と選択肢は同じメッセージウィンドウに置き、移動・調べる・帰還はコマンドとして分離します。戦闘中会話の間は戦闘操作を表示しません。

## 一覧・入力・長文

手帳・道具・隊・依頼掲示板・迷宮選択は画面内のウィンドウで開きます。一覧だけが内部スクロールし、閉じると同じ場面へ戻ります。開閉と表示切り替えだけではdispatchしません。表示中は背景の操作をinertにし、WASD・E・背面の会話送り・数字の選択を遮断します。矢印とEnterは開いている画面のボタンを操作します。Escapeで候補一覧・詳細・管理画面を一段ずつ閉じ、最後は元の場面のコマンドへ戻します。記録・ヘルプのシステムダイアログにも同じ画面内の表示範囲を適用します。

長い本文はビュー内のページ送りで読み、最後のページまでゲーム側のadvance/chooseを送りません。選択肢が多い場合はメッセージ内をスクロールして全候補と条件を読めます。ウィンドウ再描画では検索欄のフォーカス・入力位置と一覧のスクロール位置を可能な限り保持します。

## 重なりと狭い画面

背景・人物・敵、雰囲気と効果、通常UI、一覧ウィンドウの順で重ねます。照明は背景だけに掛け、文字と操作は暗くしません。効果は画面内へ切り取り、表示切り替えだけで再生し直しません。中央の人物と敵を見せる余白を取り、戦闘対象の選択をメッセージや地図で覆わない配置にします。

狭い画面はコマンドを下側へ並べ直し、隊を短い表示、地図を開閉式にします。文字を縮めて押し込まず、一覧と長い選択肢の内部スクロールを使います。テーマの色・文字サイズを両ビューで共有し、従来テーマの隊の左右指定は従来ビューだけに適用します。

## 検証

共通キー操作の追加仕様と検証は[KEYBOARD_CONTROLS.md](KEYBOARD_CONTROLS.md)を参照してください。両ビューのフォーカス、対象選択、装備・記録の操作を同じ処理で扱います。

`tests/scene-view.test.mjs` の10件で、11個の固定スナップショットの両ビュー描画、選択肢の条件と敵対象の保持、切り替えと開閉前後のセーブ完全一致、一覧中の入力遮断、Escape、長文・同文連続のページ送り、未読ページの数字選択抑止、掲示板から会話への遷移、戦闘中会話、効果の格納先と解放、検索フォーカス・カーソル・スクロールの保持を検証します。

この試験はNodeのDOMアダプターを使います。CSSの寸法・描画・ブラウザのネイティブフォーカスは模擬しません。PR #27の公開画面では「記録 → 画面配置」の表示と両配置への切り替えを確認済みです。今回の共通キー操作は未公開のため実ブラウザでは未検証です。ローカルプレビューへの接続は `ERR_BLOCKED_BY_CLIENT` で拒否されました。320〜1440px、低い横画面、本文24px、長い選択肢、多人数の戦闘で表示の収まりと全操作への到達を確認する必要があります。記録・ヘルプダイアログ、暗闇時の文字、敵のクリック領域、音声も実画面の残項目です。

実行結果は [PROGRESS.md](../PROGRESS.md)、次工程は [HANDOFF.md](../HANDOFF.md) に記録します。
