const string={type:'string',minLength:1},boolean={type:'boolean'},integer={type:'integer'},nonnegative={type:'integer',minimum:0};
const obj=(properties,required=Object.keys(properties))=>({type:'object',properties,required,additionalProperties:false});
const point=obj({x:nonnegative,y:nonnegative,z:integer});
const rules=obj({passage:boolean,water:boolean,support:boolean});
const array=items=>({type:'array',items});
export const voxelSchema=obj({version:{const:1},minZ:{type:'integer',minimum:-32,maximum:0},layers:{type:'array',minItems:1,maxItems:16,items:{type:'array',minItems:3,items:{type:'string',pattern:'^[#.]+$'}}},faces:array(obj({id:string,name:string,at:point,side:{enum:['north','east','south','west','up','down']},operable:boolean,initiallyOpen:boolean,closed:rules,open:rules,handle:point},['id','name','at','side','operable','initiallyOpen','closed','open'])),links:array(obj({id:string,name:string,kind:{enum:['ladder','stairs','vine','rope','bridge']},path:{type:'array',minItems:2,maxItems:64,items:point},bidirectional:boolean,access:{oneOf:[obj({kind:{const:'fixed'}}),obj({kind:{const:'install'},item:string,count:{type:'integer',minimum:1,maximum:99}}),obj({kind:{const:'skill'},ability:string})]}})),devices:array({oneOf:[obj({id:string,name:string,kind:{const:'pump'},at:point,target:point,amount:{type:'integer',minimum:1,maximum:30}}),obj({id:string,name:string,kind:{const:'dig'},at:point,target:point,item:string,count:{type:'integer',minimum:1,maximum:99},ability:string},['id','name','kind','at','target','item','count'])]}),initialWater:array(obj({at:point,amount:{type:'integer',minimum:1,maximum:3}}))});
const mapPoint=obj({map:string,...point.properties});
export const voxelSystemSchema=obj({use:{const:'voxel_space'},enabled:boolean,maps:{type:'array',minItems:1,uniqueItems:true,items:string},portals:array(obj({id:string,name:string,at:mapPoint,destination:mapPoint}))},['use','maps','portals']);
